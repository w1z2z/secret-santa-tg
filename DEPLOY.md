# 🚀 Инструкция по деплою бота на VPS

## 📋 Быстрый деплой

### 1. Локально (на вашем компьютере)

```bash
# Проверить изменения
git status

# Добавить все изменения
git add .

# Закоммитить
git commit -m "Описание изменений"

# Запушить на сервер
git push
```

### 2. На VPS (через SSH)

```bash
# Перейти в директорию проекта
cd /path/to/secret-santa-tg

# Получить обновления
git pull

# Пересобрать образ бота
docker-compose build bot

# Перезапустить только контейнер бота (БД не трогаем!)
docker-compose up -d bot

# Проверить статус
docker-compose ps

# Смотреть логи в реальном времени
docker-compose logs -f bot
```

### ⚡ Все в одной команде

```bash
cd /path/to/secret-santa-tg && \
git pull && \
docker-compose build bot && \
docker-compose up -d bot && \
docker-compose logs -f bot
```

---

## 🔄 Откат изменений (если что-то пошло не так)

```bash
# Найти предыдущий коммит
git log --oneline

# Откатиться на предыдущий коммит
git checkout <hash-коммита>

# Пересобрать и перезапустить
docker-compose build bot
docker-compose up -d bot
```

---

## ⚙️ Отличия локальной разработки и продакшена

### 🔧 Переменные окружения (.env)

#### Локальная разработка

```env
# Бот
TG_TOKEN=your_dev_bot_token_here

# MongoDB
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USERNAME=your_username
MONGO_PASSWORD=your_password
MONGO_DATABASE=secret-santa
MONGO_URI=mongodb://localhost:27017/secret-santa
```

#### Продакшен (VPS)

```env
# Бот
TG_TOKEN=your_production_bot_token_here

# MongoDB
MONGO_HOST=db
MONGO_PORT=27017
MONGO_USERNAME=your_username
MONGO_PASSWORD=your_password
MONGO_DATABASE=secret-santa
MONGO_URI=mongodb://db:27017/secret-santa
```

### 📝 Ключевые отличия:

1. **`MONGO_HOST`**:
   - **Локально**: `localhost` (подключение к локальной БД)
   - **Продакшен**: `db` (имя сервиса в docker-compose, контейнеры общаются через внутреннюю сеть)

2. **`TG_TOKEN`**:
   - **Локально**: токен тестового бота (для разработки)
   - **Продакшен**: токен продакшен бота (для реальных пользователей)
   - ⚠️ **ВАЖНО**: Убедитесь что используете правильный токен для каждой среды!

3. **`MONGO_URI`**:
   - **Локально**: `mongodb://localhost:27017/...`
   - **Продакшен**: `mongodb://db:27017/...` (где `db` - имя сервиса из docker-compose.yml)

### 🐳 Docker Compose

В продакшене используется `docker-compose.yml`, где:
- Сервис `db` - MongoDB контейнер
- Сервис `bot` - контейнер с ботом
- Оба сервиса находятся в одной сети `secret-santa-network`
- Бот подключается к БД по имени сервиса `db`, а не по `localhost`

---

## 🛠️ Первоначальная настройка на VPS

### 1. Клонировать репозиторий

```bash
git clone <your-repo-url>
cd secret-santa-tg
```

### 2. Создать `.env` файл

```bash
cp .env.example .env  # или создать вручную
nano .env
```

### 3. Заполнить переменные окружения

Убедитесь что указаны:
- ✅ `TG_TOKEN` - токен продакшен бота
- ✅ `MONGO_HOST=db` (не localhost!)
- ✅ `MONGO_URI=mongodb://db:27017/...` (не localhost!)
- ✅ Остальные переменные для MongoDB

### 4. Запустить всё

```bash
# Запустить БД и бота
docker-compose up -d

# Проверить что всё запустилось
docker-compose ps

# Смотреть логи
docker-compose logs -f
```

---

## 📊 Полезные команды

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Только бот
docker-compose logs -f bot

# Только БД
docker-compose logs -f db

# Последние 100 строк
docker-compose logs --tail=100 bot
```

### Управление контейнерами

```bash
# Остановить всё
docker-compose down

# Остановить только бота (БД продолжит работать)
docker-compose stop bot

# Запустить только бота
docker-compose start bot

# Перезапустить только бота
docker-compose restart bot

# Статус контейнеров
docker-compose ps
```

### Работа с БД

```bash
# Подключиться к MongoDB контейнеру
docker-compose exec db mongosh -u $MONGO_USERNAME -p $MONGO_PASSWORD

# Бэкап БД
docker-compose exec db mongodump --out /data/backup --username $MONGO_USERNAME --password $MONGO_PASSWORD

# Восстановление БД из бэкапа
docker-compose exec db mongorestore /data/backup --username $MONGO_USERNAME --password $MONGO_PASSWORD
```

---

## ⚠️ Важные замечания

1. **Никогда не коммитьте `.env` файл** - он должен быть в `.gitignore`
2. **БД не трогается при деплое** - обновляется только контейнер бота
3. **Токены должны быть разные** для локальной разработки и продакшена
4. **Проверяйте логи** после каждого деплоя: `docker-compose logs -f bot`
5. **Мониторьте статус**: `docker-compose ps` - все сервисы должны быть `Up`

---

## 🔍 Проверка после деплоя

После успешного деплоя проверьте:

1. ✅ Контейнеры запущены: `docker-compose ps`
2. ✅ Нет ошибок в логах: `docker-compose logs bot`
3. ✅ Бот отвечает на команды в Telegram
4. ✅ БД доступна и работает

---

## 📞 Если что-то пошло не так

1. Проверьте логи: `docker-compose logs bot`
2. Проверьте переменные окружения в `.env`
3. Убедитесь что токен правильный
4. Проверьте что `MONGO_HOST=db` (не localhost!)
5. Попробуйте откатиться на предыдущий коммит

